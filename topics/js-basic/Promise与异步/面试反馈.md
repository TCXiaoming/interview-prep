# 面试反馈

## 2026-04-30 综合面试记录

- 表现：⚠️ 有缺口
- 考察要点：async/await 执行时机、await bar() 的同步执行与后续微任务注册
- 回答概要：知道事件循环整体机制，但判断 `await bar()` 的输出顺序时出错——以为 bar() 调用也是异步的
- 薄弱点：不清楚 `await` 后面的表达式是被**同步执行**的，只有后续代码被包装为微任务
- 改进方向：把 `await` 理解为 `Promise.resolve().then()` 的语法糖；练习手写 Babel 编译后的 Generator + Promise 版本
