## 2026-04-02 面试记录

- 表现：⚠️ 有缺口
- 考察要点：useCallback 真正有意义的场景
- 回答概要：知道要配合 React.memo 才有意义
- 薄弱点：不知道没有 memo 时 useCallback 完全无意义；不知道 useEffect 依赖数组也是有效场景
- 改进方向：记住两个有意义的场景：① 配合 React.memo ② 函数放入 useEffect 依赖数组
