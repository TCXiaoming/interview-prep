## 2026-03-24 面试记录

- 表现：⚠️ 有缺口
- 考察要点：useMemo/useCallback 区别，useCallback 的实际使用场景
- 回答概要：纠正后能说清 useMemo 缓存计算结果，useCallback 缓存函数引用，知道配合子组件传回调的场景
- 薄弱点：不清楚 useCallback + React.memo 的配合链路，不知道单独用 useCallback 意义不大
- 改进方向：理解"新函数引用 → React.memo 比较失效 → 不必要重渲染"这条链路
