## 2026-03-25 面试记录

- 表现：⚠️ 有缺口
- 考察要点：Vue2 vs Vue3 响应式原理，Proxy 如何感知新增属性
- 回答概要：知道 Vue2 用 defineProperty、Vue3 用 Proxy，能说出新增属性和数组的痛点
- 薄弱点：不知道 Proxy set trap 捕获新增属性的具体机制
- 改进方向：了解 Proxy 各 trap 的拦截范围，重点掌握 get/set/deleteProperty

## 2026-05-05 综合面试记录

- 表现：⚠️ 有缺口
- 考察要点：Proxy 拦截操作数量、与 Object.defineProperty 对比、set trap 区分新增/修改机制
- 回答概要：能说出 Proxy 可拦截 13 种操作；能列举数组 length、新增属性、delete 等痛点；但不清楚 set trap 如何区分新增和修改属性
- 薄弱点：不知道 `key in target` / `Reflect.has(target, key)` 来区分新增/修改
- 改进方向：记住 set trap 中用 `key in target` 判断——已有属性触发该属性依赖，新增属性触发整个对象依赖，这也是 Vue.set 不再需要的原因
